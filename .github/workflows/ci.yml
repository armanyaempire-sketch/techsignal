name: TechSignal Manual Validation

on:
  workflow_dispatch:

jobs:
  validation:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Manual validation workflow. Run npm install, npm run typecheck and npm run build in a trusted environment before the production deployment."
